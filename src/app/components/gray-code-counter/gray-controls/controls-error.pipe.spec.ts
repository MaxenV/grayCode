import { ControlsErrorPipe } from './controls-error.pipe';

describe('ControlsErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlsErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
